/* eslint-disable */
/*
import { css } from '@emotion/react';
import { fontFamily, fontSize } from '../fonts';

describe('Tests for fonts', () => {
  let expectedResult;

  it('should return default font family', () => {
    expectedResult = css`
      font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
    `;
    expect(fontFamily()['styles']).toEqual(expectedResult['styles']);
  });

  it('should return default font size', () => {
    expectedResult = css`
      font-size: 14px;
    `;
    expect(fontSize()['styles']).toEqual(expectedResult['styles']);
  });
});
*/
